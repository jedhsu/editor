(let [f (. vim api)]
  (f:nvim_set_option "syntax" "enable"))

; set noshowmode

; " UTF-8
; set encoding=utf8

; " Syntax highlighting.
; syntax on

; " Disable the default Vim startup message.
; set shortmess+=I

; " Show line/relative numbers.
; set number
; set relativenumber

; " Disable continuation of comments to next line.
; set formatoptions-=cro

; set ruler
; set wildmenu

; set autoread
